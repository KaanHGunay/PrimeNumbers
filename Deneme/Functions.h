#pragma once
#include "Node.h"
#include "LinkedList.h"
#include <cmath>
#include <ctime>
#include <iostream> 

inline bool is_prime( int, Node*);
int find_prime_at( int);
int find_max_prime_up_to( int);
inline bool is_prime_until_nullptr( int, Node*);
int find_primes( int);
int find_max_prime_up_to_with_time( int);
